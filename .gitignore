# Local NDI SDK drop (if a user copies the SDK in here instead of installing
# it system-wide). The addon's CMakeLists also accepts NDI_SDK_ROOT so this
# is just a courtesy.
libs/
ndi-sdk/

# Build artifacts from compiling the bundled examples
build/
build-*/
bin/

# IDE / OS junk
.DS_Store
.vscode/
.idea/
*.xcodeproj/
